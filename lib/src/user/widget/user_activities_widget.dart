import 'package:flutter/material.dart';

class UserActivitiesWidget extends StatelessWidget {
  const UserActivitiesWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
